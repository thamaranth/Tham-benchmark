import chai, { expect } from 'chai'
import times from '../times'
import date from '../main/date'
import moment from 'moment'

  describe('date', () => {

    it('does the thing', () => {
    })

    describe('date', () => {
      it('changes the given dates to yyyy-mm-dd format', () => {
        console.log(date(times))
        // expect(date(times)).to.equal('1972-11')
      })
    })

  })
